[pytest]
testpaths = tests evaluation
# cloud-rca-demo-app/tests intentionally fail on the faulty code (they pass
# only after an approved patch). Run them explicitly:
#   pytest cloud-rca-demo-app -q        # expect failures pre-patch (by design)
# The fix pipeline runs selected demo-app tests on the patched branch.
